import type { Settings } from '~/utils/config/settings';
import { DEFAULT_SETTINGS } from '~/utils/config/settings';

/**
 * Default settings object for unit tests.
 * Use this instead of creating Settings objects inline.
 */
export const TEST_DEFAULT_SETTINGS: Settings = DEFAULT_SETTINGS;
